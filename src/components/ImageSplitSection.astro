---
import siteInfo from '../data/site-info.json';

interface Props {
  imageSamedi?: string;
  imageDimanche?: string;
}

const {
  imageSamedi = "/images/motocross-saut-2.jpg",
  imageDimanche = "/images/motocross-expert.png"
} = Astro.props;
---

<section class="categories-section">
  <div class="container">
    <div class="categories-grid">
      <!-- Colonne gauche : Texte d'introduction -->
      <div class="intro-column">
        <h2 class="section-subtitle">Les horaires et catégories</h2>
        <h3 class="section-title">BIENVENUE AU TERRAIN</h3>
        <div class="title-underline"></div>

        <p class="intro-text">
          Notre terrain de motocross accueille tous les niveaux, du débutant au pilote confirmé.
          Nous proposons des créneaux adaptés à chaque catégorie pour garantir la sécurité et
          le plaisir de tous.
        </p>

        <p class="intro-text">
          Le terrain est ouvert les <strong>{siteInfo.horaires.jours}</strong> avec des horaires
          spécifiques selon votre niveau. Une piste technique et bien entretenue vous attend pour
          progresser dans les meilleures conditions.
        </p>

        <div class="info-box">
          <h4>Informations pratiques</h4>
          <ul>
            <li>
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span>Tarif : <a href="/tarifs" class="tarif-link">{siteInfo.tarif}</a></span>
            </li>
            <li>
              <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span>{siteInfo.vehicules_acceptes.description}</span>
            </li>
          </ul>
        </div>
      </div>

      <!-- Colonne droite : Horaires détaillés -->
      <div class="categories-column">
        <!-- Ligne 1 : Image + 1er week-end -->
        <div class="category-row">
          <div class="category-image">
            <img src={imageSamedi} alt="1er week-end du mois" loading="lazy" />
          </div>
          <div class="category-info premier-weekend">
            <div class="category-badge samedi">1ER WEEK-END</div>
            <h3>SAMEDI & DIMANCHE</h3>
            <div class="category-details">
              <div class="horaire-detail">
                <div class="detail-item">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span><strong>Samedi 14h-18h</strong> - UFOLEP</span>
                </div>
                <ul class="category-list">
                  <li>Minicross</li>
                  <li>85cc débutant</li>
                </ul>
              </div>
              <div class="horaire-detail">
                <div class="detail-item">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span><strong>Dimanche 9h-18h</strong> - FFM</span>
                </div>
                <p class="pause-info">(Pause 12h30-13h30)</p>
                <ul class="category-list">
                  <li>85cc confirmés</li>
                  <li>125cc</li>
                  <li>MX1 & MX2</li>
                  <li>Quad & Side-car</li>
                </ul>
              </div>
            </div>
          </div>
        </div>

        <!-- Ligne 2 : 3ème week-end + Image -->
        <div class="category-row reverse">
          <div class="category-info troisieme-weekend">
            <div class="category-badge dimanche">3ÈME WEEK-END</div>
            <h3>SAMEDI & DIMANCHE</h3>
            <div class="category-details">
              <div class="horaire-detail">
                <div class="detail-item">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span><strong>Samedi 14h-18h</strong> - FFM</span>
                </div>
                <ul class="category-list">
                  <li>85cc confirmés</li>
                  <li>125cc</li>
                  <li>MX1 & MX2</li>
                  <li>Quad & Side-car</li>
                </ul>
              </div>
              <div class="horaire-detail">
                <div class="detail-item">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <span><strong>Dimanche 9h-18h</strong> - UFOLEP</span>
                </div>
                <p class="pause-info">(Pause 12h30-13h30)</p>
                <ul class="category-list">
                  <li>Minicross</li>
                  <li>85cc débutant</li>
                </ul>
              </div>
            </div>
          </div>
          <div class="category-image">
            <img src={imageDimanche} alt="3ème week-end du mois" loading="lazy" />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .categories-section {
    background: var(--color-dark-lighter);
    padding: 0;
  }

  .categories-section .container {
    max-width: 100%;
    padding: 0;
  }

  .categories-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    align-items: stretch;
    min-height: 450px;
  }

  /* Colonne gauche : Introduction */
  .intro-column {
    position: relative;
    padding: var(--spacing-xl) 10%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    background-image: url('/images/vue-aerienne-terrain-1.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    overflow: hidden;
  }

  .intro-column::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(10, 10, 10, 0.85) 0%, rgba(10, 10, 10, 0.75) 100%);
    z-index: 0;
  }

  .intro-column > * {
    position: relative;
    z-index: 1;
  }

  .section-title {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 800;
    color: white;
    line-height: 1.1;
    margin-bottom: var(--spacing-md);
    text-transform: uppercase;
    letter-spacing: -0.02em;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.7);
  }

  .section-subtitle {
    font-size: 1.25rem;
    font-weight: 700;
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: var(--spacing-xs);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }

  .title-underline {
    width: 60px;
    height: 4px;
    background: var(--color-primary);
    margin-bottom: var(--spacing-xl);
    border-radius: 2px;
  }

  .intro-text {
    font-size: 0.95rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: var(--spacing-md);
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }

  .intro-text strong {
    color: var(--color-primary);
    font-weight: 700;
  }

  .horaires-details {
    background: rgba(185, 28, 28, 0.1);
    border-left: 3px solid var(--color-primary);
    padding: var(--spacing-md);
    margin: var(--spacing-md) 0;
    border-radius: var(--radius-sm);
  }

  .weekend-block {
    margin-bottom: var(--spacing-md);
  }

  .weekend-block:last-child {
    margin-bottom: 0;
  }

  .weekend-block h5 {
    color: var(--color-primary);
    font-size: 0.95rem;
    font-weight: 800;
    margin-bottom: var(--spacing-xs);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .weekend-block ul {
    list-style: none;
    padding-left: 0;
    margin: 0;
  }

  .weekend-block li {
    color: rgba(255, 255, 255, 0.95);
    font-size: 0.9rem;
    line-height: 1.8;
    padding-left: var(--spacing-md);
    position: relative;
  }

  .weekend-block li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--color-primary);
    font-weight: bold;
  }

  .weekend-block li strong {
    color: white;
    font-weight: 700;
  }

  .info-box {
    background: rgba(10, 10, 10, 0.6);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    padding: var(--spacing-md);
    margin-top: var(--spacing-md);
    backdrop-filter: blur(10px);
  }

  .info-box h4 {
    font-size: 1rem;
    font-weight: 800;
    color: white;
    margin-bottom: var(--spacing-md);
    text-transform: uppercase;
  }

  .info-box ul {
    list-style: none;
  }

  .info-box li {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-sm);
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: var(--spacing-sm);
    font-size: 0.95rem;
  }

  .info-box li svg {
    width: 20px;
    height: 20px;
    color: var(--color-primary);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .info-box .tarif-link {
    color: rgba(255, 255, 255, 0.9);
    text-decoration: underline;
    transition: color var(--transition-fast);
  }

  .info-box .tarif-link:hover {
    color: var(--color-primary);
  }

  /* Colonne droite : Catégories */
  .categories-column {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .category-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    align-items: stretch;
    flex: 1;
  }

  .category-row.reverse {
    grid-template-columns: 1fr 1fr;
  }

  .category-image {
    position: relative;
    overflow: hidden;
    height: 100%;
    width: 100%;
  }

  .category-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .category-row:hover .category-image img {
    transform: scale(1.05);
  }

  .category-info {
    background: var(--color-secondary);
    padding: var(--spacing-lg);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .category-badge {
    display: inline-block;
    align-self: flex-start;
    padding: 0.4rem 1rem;
    font-weight: 700;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: var(--radius-md);
    margin-bottom: var(--spacing-sm);
  }

  .category-badge.samedi {
    background: var(--color-primary);
    color: white;
  }

  .category-badge.dimanche {
    background: var(--color-dark);
    color: white;
  }

  .category-info h3 {
    font-size: 1.1rem;
    font-weight: 800;
    color: #0a0a0a;
    margin-bottom: var(--spacing-sm);
    text-transform: uppercase;
    letter-spacing: -0.01em;
  }

  .category-desc {
    font-size: 0.85rem;
    line-height: 1.5;
    color: #333;
    margin-bottom: var(--spacing-sm);
  }

  .category-details {
    margin-top: auto;
  }

  .detail-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    color: var(--color-primary);
    font-weight: 600;
    font-size: 0.9rem;
    margin-bottom: var(--spacing-sm);
  }

  .detail-item svg {
    width: 18px;
    height: 18px;
  }

  .category-list {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-xs);
  }

  .category-list li {
    background: rgba(185, 28, 28, 0.1);
    border: 1px solid rgba(185, 28, 28, 0.2);
    padding: 0.3rem 0.7rem;
    border-radius: var(--radius-sm);
    font-size: 0.8rem;
    color: #0a0a0a;
    font-weight: 600;
  }

  .horaire-detail {
    margin-bottom: var(--spacing-sm);
  }

  .horaire-detail:last-child {
    margin-bottom: 0;
  }

  .horaire-detail .detail-item {
    margin-bottom: 6px;
  }

  .horaire-detail .detail-item span {
    font-size: 0.9rem;
  }

  .horaire-detail .detail-item span strong {
    color: var(--color-primary);
  }

  .pause-info {
    color: #666;
    font-size: 0.8rem;
    margin: 0 0 8px 0;
    padding-left: calc(18px + var(--spacing-xs));
    font-style: italic;
  }

  .category-list {
    margin-top: 8px;
  }

  @media (max-width: 1024px) {
    .categories-grid {
      grid-template-columns: 1fr;
      gap: 0;
      min-height: auto;
    }

    .intro-column {
      padding: var(--spacing-xl);
    }

    .category-row {
      grid-template-columns: 1fr 1fr;
    }

    .category-row.reverse {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 768px) {
    .intro-column {
      padding: var(--spacing-lg);
    }

    .category-row,
    .category-row.reverse {
      grid-template-columns: 1fr;
      gap: 0;
    }

    .category-image {
      min-height: 200px;
    }

    .category-info {
      padding: var(--spacing-lg);
    }

    .category-list {
      justify-content: flex-start;
    }
  }
</style>
