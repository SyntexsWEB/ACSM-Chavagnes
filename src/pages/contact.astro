---
import Layout from '../layouts/Layout.astro';
import HeroPage from '../components/HeroPage.astro';
import { getEntry } from 'astro:content';
import siteInfo from '../data/site-info.json';

const page = await getEntry('pages', 'contact');
if (!page) throw new Error('Page not found: contact');

const heroData = page.data.hero || {
  image: '/images/20250504_133808.jpg',
  titre: 'Contactez-nous',
  soustitre: 'Nous sommes à votre écoute'
};
---

<Layout
  titre={page.data.titre}
  description={page.data.description}
>
  <HeroPage
    titre={heroData.titre ?? 'Contactez-nous'}
    soustitre={heroData.soustitre ?? 'Nous sommes à votre écoute'}
    image={heroData.image}
  />

  <section class="section">
    <div class="container">
      <div class="contact-header" style="display: none;">
        <h1>Contactez-nous</h1>
        <p>Une question ? Besoin d'informations ? N'hésitez pas à nous contacter !</p>
      </div>

      <div class="contact-grid">
        <div class="contact-form-container card">
          <h2>Envoyez-nous un message</h2>
          <form class="contact-form" name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field">
            <input type="hidden" name="form-name" value="contact" />
            <p hidden>
              <label>
                Don't fill this out: <input name="bot-field" />
              </label>
            </p>

            <div class="form-group">
              <label for="nom">Nom complet *</label>
              <input type="text" id="nom" name="nom" required />
            </div>

            <div class="form-group">
              <label for="email">Email *</label>
              <input type="email" id="email" name="email" required />
            </div>

            <div class="form-group">
              <label for="telephone">Téléphone</label>
              <input type="tel" id="telephone" name="telephone" />
            </div>

            <div class="form-group">
              <label for="sujet">Sujet *</label>
              <select id="sujet" name="sujet" required>
                <option value="">Sélectionnez un sujet</option>
                <option value="inscription">Inscription / Licence</option>
                <option value="horaires">Horaires et planning</option>
                <option value="tarifs">Tarifs</option>
                <option value="partenariat">Partenariat</option>
                <option value="autre">Autre question</option>
              </select>
            </div>

            <div class="form-group">
              <label for="message">Message *</label>
              <textarea id="message" name="message" rows="6" required></textarea>
            </div>

            <button type="submit" class="btn">Envoyer le message</button>
          </form>
        </div>

        <div class="contact-info">
          <div class="info-card card">
            <h3>Informations pratiques</h3>
            {siteInfo.adresse && (
              <div class="info-item">
                <div class="info-icon">📍</div>
                <div>
                  <strong>Adresse</strong>
                  <p>{siteInfo.adresse}</p>
                </div>
              </div>
            )}
            <div class="info-item">
              <div class="info-icon">⏰</div>
              <div>
                <strong>Horaires</strong>
                <p>{siteInfo.horaires.jours}</p>
                <p>Samedi: {siteInfo.horaires.samedi}</p>
                <p>Dimanche: {siteInfo.horaires.dimanche}</p>
              </div>
            </div>
            <div class="info-item">
              <div class="info-icon">💶</div>
              <div>
                <strong>Tarif</strong>
                <p>{siteInfo.tarif} la journée</p>
              </div>
            </div>
          </div>

          <div class="info-card card">
            <h3>Avant de venir</h3>
            <ul class="checklist">
              <li>✓ Vérifier le calendrier d'ouverture</li>
              <li>✓ Avoir sa licence FFM à jour</li>
              <li>✓ Préparer son équipement de sécurité</li>
              <li>✓ Prévoir {siteInfo.tarif} pour l'entrée</li>
              <li>✓ Consulter le règlement du terrain</li>
            </ul>
            <a href="/reglement" class="btn btn-secondary" style="margin-top: var(--spacing-md); display: inline-block;">
              Voir le règlement
            </a>
          </div>

          <div class="info-card card">
            <h3>Besoin d'aide ?</h3>
            <p>Consultez notre FAQ pour trouver rapidement des réponses à vos questions les plus fréquentes.</p>
            <a href="/faq" class="btn" style="margin-top: var(--spacing-md); display: inline-block;">
              Consulter la FAQ
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .contact-header {
    text-align: center;
    margin-bottom: var(--spacing-2xl);
  }

  .contact-header h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    color: var(--color-text);
    font-weight: 700;
    margin-bottom: var(--spacing-md);
    position: relative;
    display: inline-block;
    padding-bottom: var(--spacing-sm);
  }

  .contact-header h1::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 80px;
    height: 4px;
    background: var(--color-primary);
    border-radius: 2px;
  }

  .contact-header p {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: var(--spacing-xl);
  }

  .contact-form-container h2 {
    color: var(--color-primary);
    margin-bottom: var(--spacing-lg);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .form-group {
    display: flex;
    flex-direction: column;
  }

  .form-group label {
    margin-bottom: var(--spacing-xs);
    color: var(--color-text);
    font-weight: 500;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: var(--spacing-sm);
    background: var(--color-dark);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    color: var(--color-text);
    font-family: inherit;
    font-size: 1rem;
    transition: all var(--transition-fast);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
    box-shadow: 0 0 0 3px var(--shadow-primary-subtle);
  }

  .form-group textarea {
    resize: vertical;
    min-height: 120px;
  }

  .contact-info {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .info-card h3 {
    color: var(--color-primary);
    margin-bottom: var(--spacing-md);
    font-size: 1.25rem;
  }

  .info-item {
    display: flex;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
  }

  .info-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  .info-item strong {
    display: block;
    color: var(--color-text);
    margin-bottom: 4px;
  }

  .info-item p {
    color: var(--color-text-secondary);
    margin: 0;
    font-size: 0.9rem;
  }

  .checklist {
    list-style: none;
    padding: 0;
  }

  .checklist li {
    padding: var(--spacing-xs) 0;
    color: var(--color-text-secondary);
  }

  @media (max-width: 1024px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }

    .contact-info {
      order: -1;
    }
  }
</style>
